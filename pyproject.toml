[project]
name = "stegcore"
version = "0.1.0"
description = "StegCore – constraint and governance layer for StegVerse (docs-first v0.1)"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.7.0",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
